---
title: "音频"
description: "OpenAI 兼容的语音与转写 API。"
---

| 端点 | 方法 | 说明 |
| --- | --- | --- |
| `/v1/audio/transcriptions` | POST | 语音转文字 |
| `/v1/audio/translations` | POST | 音频翻译为英文文本 |
| `/v1/audio/speech` | POST | 文字转语音 |

## 转写

```bash
curl {{api-base-url}}/audio/transcriptions \
  -H "Authorization: Bearer sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -F file=@audio.mp3 \
  -F model=whisper-1
```

## 语音合成（TTS）

```bash
curl {{api-base-url}}/audio/speech \
  -H "Authorization: Bearer sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "tts-1",
    "input": "你好，来自 Starrise API。",
    "voice": "alloy"
  }' \
  --output speech.mp3
```

```python
from openai import OpenAI

client = OpenAI(
    base_url="{{api-base-url}}",
    api_key="sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
)

with client.audio.speech.with_streaming_response.create(
    model="tts-1",
    voice="alloy",
    input="你好，来自 Starrise API。",
) as response:
    response.stream_to_file("speech.mp3")
```

## Suno 音乐

AI 音乐生成见 [Suno API](/zh/api/suno)。

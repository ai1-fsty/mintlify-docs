---
title: "GPT-5.2 Chat Latest"
description: "OpenAI GPT-5.2 Chat Latest — GPT-5.2 最新对话优化版本。"
---

GPT-5.2 Chat Latest 通过 Starrise AI 以 OpenAI Responses API (`/v1/responses`) 提供服务。

## 核心能力

* **Responses API** — 使用新版 `/v1/responses` 端点，用 `input` 替代 `messages`
* **推理控制** — 可配置推理工作量（minimal / low / medium / high）和摘要
* **详细度控制** — 设置响应详细程度为 low、medium 或 high
* **流式输出** — 通过 SSE 实现实时 Token 流式传输
* **工具调用** — 支持函数调用和工具使用

## 快速示例

<CodeGroup>
  ```bash cURL
  curl https://ai.alad.com/v1/responses \
    -H "Authorization: Bearer YOUR_API_KEY" \
    -H "Content-Type: application/json" \
    -d '{
      "model": "gpt-5.2-chat-latest",
      "input": [
        { "role": "user", "content": "用简单的语言解释量子纠缠。" }
      ],
      "reasoning": {
        "effort": "high",
        "summary": "auto"
      },
      "text": {
        "format": { "type": "text" },
        "verbosity": "medium"
      },
      "store": true
    }'
  ```

  ```python Python
  from openai import OpenAI

  client = OpenAI(
      api_key="YOUR_API_KEY",
      base_url="https://ai.alad.com/v1"
  )

  response = client.responses.create(
      model="gpt-5.2-chat-latest",
      input=[
          {"role": "user", "content": "用简单的语言解释量子纠缠。"}
      ],
      reasoning={"effort": "high", "summary": "auto"},
      text={"format": {"type": "text"}, "verbosity": "medium"},
      store=True
  )

  print(response.output[0].content[0].text)
  ```

  ```python 流式输出
  from openai import OpenAI

  client = OpenAI(
      api_key="YOUR_API_KEY",
      base_url="https://ai.alad.com/v1"
  )

  stream = client.responses.create(
      model="gpt-5.2-chat-latest",
      input=[
          {"role": "user", "content": "写一首关于大海的短诗。"}
      ],
      stream=True
  )

  for event in stream:
      if hasattr(event, 'delta'):
          print(event.delta, end="")
  ```
</CodeGroup>

## 参数说明

| 参数                  | 类型             | 必填 | 说明                                   |
| ------------------- | -------------- | -- | ------------------------------------ |
| `model`             | string         | 是  | 固定为 `gpt-5.2-chat-latest`            |
| `input`             | array          | 是  | `{ role, content }` 对象数组             |
| `stream`            | boolean        | 否  | 开启 SSE 流式传输，默认 `false`               |
| `temperature`       | float          | 否  | `0`\u20132，控制随机性，默认 `1`              |
| `top_p`             | float          | 否  | 核采样阈值，默认 `1`                         |
| `max_output_tokens` | integer        | 否  | 最大输出 Token 数                         |
| `stop`              | string / array | 否  | 触发停止生成的序列                            |
| `reasoning`         | object         | 否  | `{ effort, summary }` — 控制推理深度       |
| `text`              | object         | 否  | `{ format, verbosity }` — 控制输出格式和详细度 |
| `tools`             | array          | 否  | 模型可调用的工具列表                           |
| `store`             | boolean        | 否  | 存储响应以供后续检索，默认 `true`                 |

<Card title="API 参考" icon="code" href="/zh/api-reference/model-api/openai/gpt-5-2-chat-latest">
  查看 GPT-5.2 Chat Latest 的交互式 API Playground。
</Card>

---
title: "Audio"
description: "OpenAI-compatible speech-to-text, text-to-speech, and audio translation APIs."
---

| Endpoint | Method | Description |
| --- | --- | --- |
| `/v1/audio/transcriptions` | POST | Speech-to-text (transcription) |
| `/v1/audio/translations` | POST | Translate audio to English text |
| `/v1/audio/speech` | POST | Text-to-speech generation |

## Transcription (Speech-to-Text)

Convert audio to text using Whisper models:

```bash
curl {{api-base-url}}/audio/transcriptions \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -F file=@audio.mp3 \
  -F model=whisper-1 \
  -F language=en
```

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- |
| `file` | file | Yes | Audio file (mp3, mp4, mpeg, mpga, m4a, wav, webm) |
| `model` | string | Yes | Model ID (e.g. `whisper-1`) |
| `language` | string | No | ISO 639-1 language code |
| `prompt` | string | No | Guide transcription style |
| `response_format` | string | No | `json`, `text`, `srt`, `verbose_json`, `vtt` |
| `temperature` | number | No | Sampling temperature (0–1) |

## Speech (Text-to-Speech)

Generate spoken audio from text:

```bash
curl {{api-base-url}}/audio/speech \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "tts-1",
    "input": "Hello from Starrise AI.",
    "voice": "alloy",
    "speed": 1.0
  }' \
  --output speech.mp3
```

### Python SDK

```python
from openai import OpenAI

client = OpenAI(
    base_url="{{api-base-url}}",
    api_key="YOUR_API_KEY",
)

with client.audio.speech.with_streaming_response.create(
    model="tts-1",
    voice="alloy",
    input="Hello from Starrise AI.",
) as response:
    response.stream_to_file("speech.mp3")
```

### Parameters

| Parameter | Type | Required | Description |
| --- | --- | --- |
| `model` | string | Yes | TTS model (e.g. `tts-1`, `tts-1-hd`) |
| `input` | string | Yes | Text to convert to speech |
| `voice` | string | Yes | Voice: `alloy`, `echo`, `fable`, `onyx`, `nova`, `shimmer` |
| `speed` | number | No | Speed multiplier (0.25–4.0), default `1.0` |
| `response_format` | string | No | `mp3`, `opus`, `aac`, `flac`, `wav`, `pcm` |

## Translation

Translate audio to English text:

```bash
curl {{api-base-url}}/audio/translations \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -F file=@audio_fr.mp3 \
  -F model=whisper-1
```

## Suno Music

For AI music generation, see [Suno API](/en/api/suno).

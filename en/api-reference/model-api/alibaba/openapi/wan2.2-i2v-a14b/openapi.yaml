openapi: 3.1.0
info:
  title: Wan2.2 I2V A14B
  description: Call the Alibaba Wan2.2 image-to-video model (asynchronous task)
  version: 1.0.0
servers:
  - url: https://ai.alad.com
security:
  - bearerAuth: []
paths:
  /v1/video/generations:
    post:
      summary: Create an image-to-video task
      description: >
        Generate a video from a reference image and a text description. This is an asynchronous task — after submitting, poll `GET /v1/video/generations/{task_id}`
        until `status` is `succeeded`.
      operationId: createWan22I2v
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - model
                - image
              properties:
                model:
                  type: string
                  enum:
                    - wan2.2-i2v-a14b
                  description: model ID
                  example: wan2.2-i2v-a14b
                image:
                  type: string
                  description: Public URL of the reference image.
                  example: https://example.com/reference.jpg
                prompt:
                  type: string
                  description: Video description text (optional, used to guide generation).
                  example: The camera slowly dollies in
                duration:
                  type: number
                  description: Video duration in seconds, 3–5.
                  minimum: 3
                  maximum: 5
                  default: 5
                width:
                  type: integer
                  description: Output video width in pixels.
                height:
                  type: integer
                  description: Output video height in pixels.
                seed:
                  type: integer
                  description: Random seed, used to reproduce results.
      responses:
        '200':
          description: Task created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  task_id:
                    type: string
                    description: Task ID, used to poll status.
                  status:
                    type: string
        '400':
          description: Invalid request parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
components:
  schemas:
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer

